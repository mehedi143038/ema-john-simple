import React from 'react';

const Inventory = () => {

    const handleAddEvent = () =>{
        
    }

    return (
        <div>
            <h2>Add inventory to sell more...</h2>
            <button onClick={handleAddEvent}>Add Inventory</button>
        </div>
    );
};

export default Inventory;